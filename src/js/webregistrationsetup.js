import React from 'react';
import ReactDom from 'react-dom';

import PageWebRegistrationSetup from '../pages/PageWebRegistrationSetup';

ReactDom.render(
  <PageWebRegistrationSetup />,
  document.getElementById('app')
);
